function solve(inputArr){
        
        const [username ,... rest]=inputArr;
        const userPass=new Array (username.length).split().map(char 
        console.log();
}

solve(['Acer','login','go','let me in','recA']);