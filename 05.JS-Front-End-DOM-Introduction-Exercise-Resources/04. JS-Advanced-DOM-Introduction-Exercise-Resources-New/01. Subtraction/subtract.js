function subtract() {
     const firstEl=document.getElementById('firstNumber');
     const secondEl=document.getElementById('secondNumber');
     document.getElementById('result').textContent= Number(firstEl.value)-Number(secondEl.value);
    
}